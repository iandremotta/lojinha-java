create table usuarios(
   id serial primary key,
   email varchar(80),
   senha varchar(50)
);

create table produtos(
   id serial primary key,
   descricao varchar(255),
   preco DECIMAL(16,2),
   unidade varchar(100)
);

insert into usuarios values (1, 'admin@gmail.com', 'admin') ;